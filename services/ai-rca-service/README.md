# AI RCA Service

Python FastAPI service for root-cause analysis workflows.

Current foundation endpoints:

- `GET /health`
- `GET /metrics`
- `POST /analyze-incident`

The analysis endpoint returns a deterministic mock RCA response until AI integrations are added.

