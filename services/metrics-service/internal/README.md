# Internal Packages

The metrics service keeps implementation packages under `internal/` so future metric validation, aggregation, Kafka publishing, and Prometheus instrumentation stay behind the service boundary.

