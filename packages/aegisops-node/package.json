{
  "name": "@aegisops/node",
  "version": "0.1.0",
  "description": "AegisOps Node.js SDK for Express telemetry ingestion",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "express": ">=4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "express": "^4.19.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
